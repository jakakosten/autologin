<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <!-- CSS only -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      integrity="sha512-iecdLmaskl7CVkqkXNQ/ZH/XLlvWZOJyj7Yy7tcenmpD1ypASozpmT/E0iPtmFIB46ZmdtAc9eNBvH0H/ZpiBw=="
      crossorigin="anonymous"
      referrerpolicy="no-referrer"
    />

    <link rel="stylesheet" href="./style.css" />
  </head>
  <body>
    <div class="container mt-4">
      <!-- Section: Design Block -->
      <section class="background-radial-gradient overflow-hidden">
        <style>
          /* ===== Google Font Import - Poppins ===== */
          @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400;500;600&display=swap");
          * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
          }
          body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #4070f4;
          }
          .container {
            position: relative;
            height: max-content;
            max-width: 80vw;
            width: 100%;
            border-radius: 6px;
            padding: 30px;
            margin: 0 15px;
            background-color: #fff;
          }
          .container header {
            position: relative;
            font-size: 20px;
            font-weight: 600;
            color: #333;
          }
          .container header::before {
            content: "";
            position: absolute;
            left: 0;
            bottom: -2px;
            height: 3px;
            width: 27px;
            border-radius: 8px;
            background-color: #4070f4;
          }
          .container form {
            position: relative;
            margin-inline: auto;
            margin-top: 16px;
            min-height: 490px;
            background-color: #fff;
            overflow: hidden;
          }
          .container form .form {
            position: absolute;
            background-color: #fff;
            transition: 0.3s ease;
          }
          .container form .form.second {
            opacity: 0;
            pointer-events: none;
            transform: translateX(100%);
          }
          form.secActive .form.second {
            opacity: 1;
            pointer-events: auto;
            transform: translateX(0);
          }
          form.secActive .form.first {
            opacity: 0;
            pointer-events: none;
            transform: translateX(-100%);
          }
          .container form .title {
            display: block;
            margin-bottom: 8px;
            font-size: 16px;
            font-weight: 500;
            margin: 6px 0;
            color: #333;
          }
          .container form .fields {
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
            width: 70vw;
            max-width: 1300px;
          }
          form .fields .input-field {
            display: flex;
            width: calc(100% / 3 - 15px);
            flex-direction: column;
            margin: 4px 0;
          }
          .input-field label {
            font-size: 12px;
            font-weight: 500;
            color: #2e2e2e;
          }
          .phoneGroup {
            display: flex;
            flex-direction: row;
          }
          .input-field input,
          select {
            outline: none;
            font-size: 14px;
            font-weight: 400;
            color: #333;
            border-radius: 5px;
            border: 1px solid #aaa;
            padding: 0 15px;
            height: 42px;
            margin: 8px 0;
          }
          .input-field input :focus,
          .input-field select:focus {
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.13);
          }
          .input-field select,
          .input-field input[type="date"] {
            color: #707070;
          }
          .input-field input[type="date"]:valid {
            color: #333;
          }
          .container form button,
          .backBtn {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 45px;
            max-width: 200px;
            width: 100%;
            border: none;
            outline: none;
            color: #fff;
            border-radius: 5px;
            margin: 25px 0;
            background-color: #4070f4;
            transition: all 0.3s linear;
            cursor: pointer;
          }
          .container form .btnText {
            font-size: 14px;
            font-weight: 400;
          }

          form input {
            margin-inline: 0.2em !important;
          }

          form button:hover {
            background-color: #265df2;
          }
          form button i,
          form .backBtn i {
            margin: 0 6px;
          }
          form .backBtn i {
            transform: rotate(180deg);
          }
          form .buttons {
            display: flex;
            align-items: center;
          }
          form .buttons button,
          .backBtn {
            margin-right: 14px;
          }

          @media (max-width: 750px) {
            .container form {
              overflow-y: scroll;
            }
            .container form::-webkit-scrollbar {
              display: none;
            }
            form .fields .input-field {
              width: calc(100% / 2 - 15px);
            }
          }

          @media (max-width: 550px) {
            form .fields .input-field {
              width: 100%;
            }
          }

          .password-container {
            position: relative;
            display: flex;
          }

          #toggle-icon {
            position: absolute;
            right: 1rem;
            top: 1.3em;
          }

          #toggle-iconConfirm {
            position: absolute;
            right: 1rem;
            top: 1.3em;
          }

          #toggle-icon-eA {
            position: absolute;
            right: 1rem;
            top: 1.3em;
          }

          #toggle-icon-eAConf {
            position: absolute;
            right: 1rem;
            top: 1.3em;
          }
        </style>

        <div class="container">
          <form action="/registracija" method="POST">
            <% if (messages.error) { %>
            <div class="alert alert-danger">
              <strong><%= messages.error %> </strong>
            </div>
            <% } %>
            <span class="title">Osebni podatki</span>

            <div class="fields">
              <div class="input-field">
                <label for="name">Ime in priimek</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  class="form-control"
                  placeholder="Vnesi ime in priimek"
                  required
                />
              </div>

              <div class="input-field">
                <label for="gender">Spol</label>
                <select id="gender" name="gender" class="form-control" required>
                  <option disabled selected>
                    Izberi spol <i class="fa-regular fa-caret-down"></i>
                  </option>
                  <option value="male">Moški</option>
                  <option value="female">Ženska</option>
                </select>
              </div>

              <div class="input-field">
                <label for="phone">Telefon</label>
                <input
                  type="text"
                  id="phone"
                  name="phoneNumber"
                  class="form-control"
                  placeholder="Vnesi svojo telefonsko številko"
                  required
                />
              </div>

              <div class="input-field">
                <label for="email">Email</label>
                <input
                  type="text"
                  id="email"
                  name="email"
                  class="form-control"
                  placeholder="Vnesi svoj email"
                  required
                />
              </div>

              <div class="input-field">
                <label for="passwordField">Geslo</label>
                <div class="password-container">
                  <input
                    type="password"
                    id="passwordField"
                    name="password"
                    class="form-control"
                    placeholder="Vnesi geslo za prijavo"
                    autocomplete="off"
                    required
                  />
                  <i
                    class="fa-solid fa-eye"
                    id="toggle-icon"
                    onclick="togglePassword('passwordField', 'toggle-icon')"
                  ></i>
                </div>
              </div>

              <div class="input-field">
                <label for="passwordField-confirm">Ponovi geslo</label>
                <div class="password-container">
                  <input
                    type="password"
                    id="passwordField-confirm"
                    name="passwordConfirm"
                    class="form-control"
                    placeholder="Vnesi geslo za prijavo"
                    autocomplete="off"
                    required
                  /><i
                    class="fa-solid fa-eye"
                    id="toggle-iconConfirm"
                    onclick="togglePassword('passwordField-confirm', 'toggle-iconConfirm')"
                  ></i>
                </div>
              </div>
            </div>

            <div class="eA-details">
              <span class="title">eAsistent podatki</span>

              <div class="fields">
                <div class="input-field">
                  <label for="eAusername">eA uporabniško ime</label>
                  <input
                    type="text"
                    id="eAusername"
                    name="eAusername"
                    class="form-control"
                    placeholder="Vnesi eA uporabniško ime"
                    required
                  />
                </div>

                <div class="input-field">
                  <label for="eApassword">eA geslo</label>
                  <div class="password-container">
                    <input
                      type="password"
                      id="eApassword"
                      name="eApassword"
                      class="form-control"
                      placeholder="Vnesi svoje eA geslo"
                      required
                    />
                    <i
                      class="fa-solid fa-eye"
                      id="toggle-icon-eA"
                      onclick="togglePassword('eApassword', 'toggle-icon-eA')"
                    ></i>
                  </div>
                </div>

                <div class="input-field">
                  <label for="eApasswordConfirm">Ponovi eA geslo</label>
                  <div class="password-container">
                    <input
                      type="password"
                      id="eApasswordConfirm"
                      name="eApasswordConfirm"
                      class="form-control"
                      placeholder="Vnesi svoje eA geslo"
                      required
                    />
                    <i
                      class="fa-solid fa-eye"
                      id="toggle-icon-eAConf"
                      onclick="togglePassword('eApasswordConfirm', 'toggle-icon-eAConf')"
                    ></i>
                  </div>
                </div>

                <div class="input-field">
                  <label for="birthYear">Letnica rojstva</label>
                  <select id="birthYear" name="birthYear" class="form-control">
                    <option value="default" disabled selected>
                      Izberi leto rojstva
                    </option>
                  </select>
                </div>

                <div class="input-field">
                  <label for="className">Izberi razred</label>
                  <select id="className" name="className" class="form-control">
                    <option value="default" disabled selected>
                      Izberi svoj trenutni razred
                    </option>
                    <option value="1. A">1. A</option>
                    <option value="1. B">1. B</option>
                    <option value="1. C">1. C</option>
                    <option value="1. D">1. D</option>
                    <option value="1. E">1. E</option>
                    <option value="2. A">2. A</option>
                    <option value="2. B">2. B</option>
                    <option value="2. C">2. C</option>
                    <option value="2. D">2. D</option>
                    <option value="2. E">2. E</option>
                    <option value="3. A">3. A</option>
                    <option value="3. B">3. B</option>
                    <option value="3. C">3. C</option>
                    <option value="3. D">3. D</option>
                    <option value="4. A">4. A</option>
                    <option value="4. B">4. B</option>
                    <option value="4. C">4. C</option>
                    <option value="4. D">4. D</option>
                  </select>
                </div>
              </div>
            </div>
            <button class="submit">Registriraj me</button>
          </form>
        </div>
      </section>
      <!-- Section: Design Block -->
    </div>

    <!-- JavaScript Bundle with Popper -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-pprn3073KE6tl6bjs2QrFaJGz5/SUsLqktiwsUTF55Jfv3qYSDhgCecCxMW52nD2"
      crossorigin="anonymous"
    ></script>
    <script>
      function togglePassword(fieldId, toggleIconId) {
        const passwordField = document.getElementById(fieldId);
        const toggleIcon = document.getElementById(toggleIconId);

        if (passwordField.type === "password") {
          passwordField.type = "text";
          toggleIcon.className = "fa fa-eye-slash";
        } else {
          passwordField.type = "password";
          toggleIcon.className = "fa fa-eye";
        }
      }

      const select = document.getElementById("birthYear");
      const currentYear = new Date().getFullYear();
      const maxYear = currentYear - 14;
      const minYear = maxYear - 6;

      for (let year = maxYear; year >= minYear; year--) {
        var option = document.createElement("option");
        option.text = year;
        option.value = year;
        select.appendChild(option);
      }
    </script>
  </body>
</html>
